# Analysis

> **Not yet written — no data has been collected.** The structure below
> is what this file has to contain to score full marks; delete this
> banner once it is real.

## Raw data

See `data/participants.csv` for the per-participant table and
`data/events.ndjson` for the full event log.

<!-- TODO: paste the participants table here as markdown too, so the
     file reads on its own. -->

## Charts

<!-- TODO: one or two charts. Suggested pair:
     1. comfort rating vs. ride speed, one point per (participant, view)
     2. dwell time per view, grouped bar, one group per participant
     Save them into Study/ and embed them here. -->

## Findings

<!-- TODO: one page, prose. What the numbers actually say, including
     anything that did not replicate or that you expected and did not
     find. Report the null results too. -->

## Proposed change

<!-- TODO: exactly one change to the demo, cited against the data.
     Name the metric, the numbers, and the change. -->
